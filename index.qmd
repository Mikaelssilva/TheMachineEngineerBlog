---
title: "The Machine Learning Engineer"
subtitle: "Explorando o mundo da IA e Machine Learning"
author: "Mikael Silva"
listing:
  contents: posts
  sort: "date desc"
  type: default
  categories: true
  sort-ui: true
  filter-ui: true
page-layout: full
title-block-banner: true
---

```{=html}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Aguarda um pouco para garantir que o listing foi renderizado
    setTimeout(function() {
        // Tenta encontrar diferentes possíveis containers do listing
        const listingDiv = document.querySelector('.quarto-listing') || 
                          document.querySelector('#listing-posts') || 
                          document.querySelector('.listing') ||
                          document.querySelector('[data-listing]') ||
                          document.querySelector('.quarto-listing-container') ||
                          document.querySelector('main .column-body');
        
        if (listingDiv) {
            // Cria o HTML do newsletter
            const newsletterHTML = `
                <div class="newsletter-section" style="text-align: center; margin: 40px 0; padding: 25px; background: rgba(255,255,255,0.05); border-radius: 10px; border: 1px solid rgba(255,255,255,0.1);">
                    <p style="color: #f0f0f0; font-size: 1.3em; font-weight: 600; margin-bottom: 10px;">
                        📧 Inscreva-se na Newsletter!
                    </p>
                    <p style="color: #cccccc; font-size: 0.95em; margin-bottom: 20px;">
                        Receba notificações sobre novos posts diretamente no seu email!
                    </p>
                    <a href="http://eepurl.com/jj-mXo" 
                       target="_blank" 
                       rel="noopener noreferrer"
                       class="mailchimp-button">
                        Inscrever-se
                    </a>
                </div>
            `;
            
            // Adiciona depois do listing
            listingDiv.insertAdjacentHTML('afterend', newsletterHTML);
            console.log('Newsletter adicionado com sucesso!');
        } else {
            console.log('Container do listing não encontrado');
            // Fallback: adiciona no final do main content
            const mainContent = document.querySelector('main') || document.querySelector('.page-columns');
            if (mainContent) {
                const newsletterHTML = `
                    <div class="newsletter-section" style="text-align: center; margin: 40px 0; padding: 25px; background: rgba(255,255,255,0.05); border-radius: 10px; border: 1px solid rgba(255,255,255,0.1);">
                        <p style="color: #f0f0f0; font-size: 1.3em; font-weight: 600; margin-bottom: 10px;">
                            📧 Inscreva-se Newsletter!
                        </p>
                        <p style="color: #cccccc; font-size: 0.95em; margin-bottom: 20px;">
                            Receba notificações sobre novos posts diretamente no seu email!
                        </p>
                        <a href="http://eepurl.com/jj-mXo" 
                           target="_blank" 
                           rel="noopener noreferrer"
                           class="mailchimp-button">
                            Inscrever-se
                        </a>
                    </div>
                `;
                mainContent.insertAdjacentHTML('beforeend', newsletterHTML);
                console.log('Newsletter adicionado no fallback!');
            }
        }
    }, 500); // Aguarda 500ms para garantir que tudo foi carregado
});
</script>
```